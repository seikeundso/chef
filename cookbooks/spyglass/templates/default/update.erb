#!/usr/bin/bash
# ----------------------------------------------------------------------------

set -euo pipefail

# ----------------------------------------------------------------------------

DB=spyglass

export LUA_PATH="/srv/spyglass.openstreetmap.org/osm2pgsql-themepark/lua/?.lua;;"

# ----------------------------------------------------------------------------

osm2pgsql-replication update -d "$DB"

osm2pgsql-gen -d "$DB" -j16 -a

# ----------------------------------------------------------------------------

